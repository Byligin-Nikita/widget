<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Calendar.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Планер">

    <Grid x:Name="RootGrid" Background="{StaticResource WidgetBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Minimal drag bar: a centred grab-handle + ghost minimize -->
        <Grid Grid.Row="0" Background="Transparent"
              PointerPressed="DragBar_PointerPressed"
              PointerMoved="DragBar_PointerMoved"
              PointerReleased="DragBar_PointerReleased"
              PointerCaptureLost="DragBar_PointerReleased">
            <Border Width="36" Height="4" CornerRadius="2" Opacity="0.5"
                    Background="{StaticResource SecondaryTextBrush}"
                    HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <Button x:Name="MinimizeBtn" Click="MinimizeBtn_Click"
                    HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,6,0"
                    Width="32" Height="22" Padding="0"
                    Background="Transparent" BorderThickness="0"
                    ToolTipService.ToolTip="Свернуть в трей">
                <FontIcon Glyph="&#xE921;" FontSize="10"/>
            </Button>
        </Grid>

        <NavigationView x:Name="NavView" Grid.Row="1" Background="Transparent"
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="False"
                        PaneDisplayMode="LeftCompact"
                        IsPaneToggleButtonVisible="False"
                        SelectionChanged="NavView_SelectionChanged"
                        ItemInvoked="NavView_ItemInvoked">

            <NavigationView.MenuItems>
                <NavigationViewItem Tag="ClockDate" Content="Время" Icon="Clock"/>
                <NavigationViewItem Tag="Calendar" Content="Календарь" Icon="Calendar"/>
                <NavigationViewItem Tag="Tasks" Content="Задачи" Icon="List"/>
                <NavigationViewItem Tag="Reminders" Content="Напоминания" Icon="Important"/>
                <NavigationViewItem Tag="Notes" Content="Заметки" Icon="Document"/>
            </NavigationView.MenuItems>

            <NavigationView.FooterMenuItems>
                <NavigationViewItem Tag="Settings" Content="Настройки" Icon="Setting"/>
            </NavigationView.FooterMenuItems>

            <Frame x:Name="ContentFrame"/>
        </NavigationView>
    </Grid>
</Window>
