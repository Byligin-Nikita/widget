<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Calendar.Pages.RemindersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Calendar.Pages">

    <Grid Padding="12" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Напоминания" Style="{StaticResource SectionTitleStyle}"/>

        <StackPanel Grid.Row="1" Spacing="8">
            <TextBox x:Name="NewReminderBox" PlaceholderText="Текст напоминания..."/>
            <StackPanel Orientation="Horizontal" Spacing="8">
                <CalendarDatePicker x:Name="DatePicker" HorizontalAlignment="Stretch" Width="140"/>
                <TimePicker x:Name="TimePicker" ClockIdentifier="24HourClock"/>
                <Button Content="+" Click="AddReminder_Click" Style="{StaticResource AccentButtonStyle}"/>
            </StackPanel>
        </StackPanel>

        <TextBlock x:Name="EmptyText" Grid.Row="2" Text="Нет активных напоминаний"
                   Foreground="{StaticResource SecondaryTextBrush}"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Visibility="Collapsed"/>

        <ListView x:Name="RemindersList" Grid.Row="2" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:ReminderRow">
                    <Grid Padding="8" ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="{x:Bind Title}" FontWeight="SemiBold"/>
                            <TextBlock Text="{x:Bind WhenText}" FontSize="12"
                                       Foreground="{StaticResource SecondaryTextBrush}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="4">
                            <Button Content="5м" Click="Snooze5_Click" Tag="{x:Bind Id}" FontSize="10" Padding="6,2"/>
                            <Button Content="15м" Click="Snooze15_Click" Tag="{x:Bind Id}" FontSize="10" Padding="6,2"/>
                            <Button Content="30м" Click="Snooze30_Click" Tag="{x:Bind Id}" FontSize="10" Padding="6,2"/>
                            <Button Content="✓" Click="Done_Click" Tag="{x:Bind Id}" FontSize="10" Padding="6,2"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
