<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Calendar.Pages.CalendarPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="12" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Календарь" Style="{StaticResource SectionTitleStyle}"/>
        <CalendarView x:Name="MonthCalendar" Grid.Row="1" HorizontalAlignment="Stretch"
                      SelectedDatesChanged="MonthCalendar_SelectedDatesChanged"/>
        <ListView x:Name="DayItemsList" Grid.Row="2" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Spacing="8" Padding="4">
                        <FontIcon Glyph="&#xE73E;" FontSize="14" Foreground="{StaticResource AccentBrush}"/>
                        <TextBlock Text="{Binding Title}" TextWrapping="WrapWholeWords"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
